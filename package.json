{
  "name": "tube-sprint-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run build && npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
